apiVersion: v1
kind: Service
metadata:
  name: {{ include "gatsby.fullname" . }}
  labels:
    {{- include "gatsby.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gatsby.selectorLabels" . | nindent 4 }}
