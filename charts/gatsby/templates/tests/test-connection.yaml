apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gatsby.fullname" . }}-test-connection"
  labels:
    {{- include "gatsby.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gatsby.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
